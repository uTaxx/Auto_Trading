# 이 저장소에서 일할 때

## 말투

- 자연스러운 한국어로 쓴다. 영어 문장 구조를 그대로 옮기지 않는다.
- 보고는 결론부터 쓴다. 순서는 결론, 근거, 남은 문제다.
- 전문 용어는 처음 쓸 때 한 줄로 뜻을 붙인다. 예: "슬리피지(사겠다고
  판단한 값과 실제로 산 값의 차이)".
- 숫자를 낼 때는 어느 조건(종목, 기간, 비용 가정)에서 나온 것인지 붙인다.
- 표는 숫자를 비교할 때만 쓴다. 설명을 표로 만들지 않는다.
- 줄표(—)를 쓰지 않는다. 쉼표와 마침표로 끊는다.
- 비유를 쓰지 않는다. 있는 그대로 쓴다.

## 확인하지 않고 단정하지 않는다

- 숫자나 동작을 말하기 전에 코드나 데이터를 연다.
- 확인하지 못한 것은 확인하지 못했다고 적는다. 그럴듯하게 적는 것보다
  모른다고 적는 쪽이 낫다.

## muwon406과 분리

- 이 저장소는 muwon406(국내 단기 매매 시스템)과 계좌, 상태 저장, 자동
  실행을 공유하지 않는다.
- 텔레그램 봇, 구글 드라이브 폴더, GitHub Actions를 muwon406과 따로 쓴다.
- muwon406에서 겪은 사고(공개 저장소 로그에 인증 키 노출, 전략을 바꾸는
  순간 무관한 종목이 같이 청산된 것 등)는 이 저장소에도 그대로 적용될 수
  있는 교훈이니 같은 실수를 반복하지 않는다.

## 최소 구조

- 요청받은 기능만 하나씩 추가한다. 쓰지 않는 워크플로나 추상화를 미리
  만들지 않는다.
- 실제 주문에 영향을 주는 결정(킬스위치, 인증 키 발급)은 사람이 한다.
